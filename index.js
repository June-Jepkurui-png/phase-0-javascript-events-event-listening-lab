function addingEventListener() {
    const button = document.getElementById('button'); // Replace with your actual element ID or reference
    
    button.addEventListener('click', function() {
        // Handle click event
    });

    // Optionally, return something indicating success, e.g., true
    return true; // This is just an example return value
}
